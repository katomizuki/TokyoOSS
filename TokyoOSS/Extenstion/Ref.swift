import Foundation
import Firebase
let ref_user = Firestore.firestore().collection("users")
let ref_post = Firestore.firestore().collection("memo")
let ref_ios = Firestore.firestore().collection("ios-test")
